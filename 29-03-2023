question 1 checking the largest number in the given array 
#include <stdio.h>

int main() {
    int arr[100], n, largest1, largest2;
    
    // take input from user
    printf("Enter the size of the array: ");
    scanf("%d", &n);
    printf("Enter %d elements in the array:\n", n);
    for(int i=0; i<n; i++) {
        scanf("%d", &arr[i]);
    }
    
    // find the largest element
    largest1 = arr[0];
    for(int i=1; i<n; i++) {
